<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_activity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.rosehulman.finngw.quicknotes.adapters.LoginActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/logo_image"
        android:layout_marginTop="80dp"
        android:id="@+id/logo"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:hint="Email"
        android:textColorHint="@android:color/darker_gray"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:textSize="20sp"
        android:ems="10"
        android:id="@+id/email_edit_text"
        android:layout_below="@+id/logo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:textColorHint="@android:color/darker_gray"
        android:textSize="20sp"
        android:hint="Password"
        android:ems="10"
        android:id="@+id/password_edit_text"
        android:layout_marginBottom="30dp"
        android:layout_above="@+id/email_login_button"
        android:layout_alignStart="@+id/email_edit_text"
        android:layout_alignEnd="@+id/email_edit_text" />

    <TextView
        android:text="@string/sign_in_agreement"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sign_in_agreement"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textSize="9sp" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/gmail_login_button_1x"
        android:id="@+id/gmail_login_button"
        android:background= "@android:color/transparent"
        android:layout_marginBottom="70dp"
        android:layout_above="@+id/sign_in_agreement"
        android:layout_alignEnd="@+id/email_login_button" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background= "@android:color/transparent"
        app:srcCompat="@drawable/email_login_button_1x"
        android:layout_marginBottom="66dp"
        android:layout_marginTop="100dp"
        android:id="@+id/email_login_button"
        android:layout_alignBottom="@+id/gmail_login_button"
        android:layout_centerHorizontal="true" />

</RelativeLayout>